CREATE DATABASE IF NOT EXISTS vectra_db;

USE vectra_db;

CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_completo VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL UNIQUE,
    funcao ENUM('admin', 'operador') NOT NULL,
    senha VARCHAR(255) NOT NULL,
    data_registro TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO usuarios (nome_completo, email, funcao, senha) 
VALUES 
('Admin Master', 'admin@gmail.com', 'admin', '12345678'),
('Carlos Silva', 'carlos.silva@vectra.com', 'operador', '12345678'),
('Ana Costa', 'ana.costa@vectra.com', 'operador', '12345678');

SELECT * FROM usuarios;